require 'rails_helper'

describe EventfulEvent do
  let(:eventful_event) { EventfulEvent.new(token, api_response_event) }
  let(:api_response_event) {  

  describe "attributes" do
    subject { eventful_event }
  end
end
