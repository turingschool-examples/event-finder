h1= #{@results.count} events for March

- @results.each do |result|
  .event
    = result.name
    = result.date
    = result.description
    = link_to result.venue_name, "#{result.venue_url}" 
    = result.venue_address
    
